# Руководство: Панель Агента и Разработчика

## Обзор

Панель Агента и Разработчика - это универсальный интерфейс для управления проектом через AI-агента и инструменты разработки.

## Структура интерфейса

### Две основные вкладки

#### 1. **Агент** 🤖
Режим для работы с AI-агентом, который может:
- Просматривать данные на Google Drive
- Отвечать на вопросы о проекте
- Выполнять команды (добавлять строки в таблицы, обновлять данные)

#### 2. **Разработчик** 👨‍💻
Режим для разработки новых функций с инструментами:
- Создание новых функций через чат
- Просмотр логов разработки
- Интеграция с Git

---

## Функции режима "Агент"

### Вкладка "Чат" 💬

**Назначение:** Общение с AI-агентом для выполнения задач в проекте

**Функционал:**
- Отправка текстовых команд агенту
- Прикрепление документов к запросам (кнопка 📎)
- Агент имеет доступ к Google Drive и может читать/изменять данные

**Примеры команд:**
```
"Добавь новую строку в таблицу SK с данными: Название, Артикул, Цена"
"Покажи последние 5 записей из Sheet1"
"Обнови цену для артикула ABC123 на 1500"
```

**Как это работает:**
1. Агент получает ваш запрос
2. Использует Gemini API для анализа намерения
3. Выполняет действие через Google Sheets API
4. Отвечает на естественном языке

---

### Вкладка "История" 📚

**Назначение:** Сохранение и загрузка истории чатов с агентом

**Функционал:**
- Кнопка "💾 Сохранить текущий чат" - сохраняет активную сессию
- Список сохраненных сессий с информацией:
  - Название (автоматическое: "Сессия SK - 08.12.2025, 19:30")
  - Количество сообщений
  - Дата создания
- Клик по сессии загружает все сообщения в чат

**Хранение:**
- Локально (в состоянии React)
- **TODO:** Интеграция с Google Drive для постоянного хранения

---

### Вкладка "Управление" ⚙️

**Назначение:** Быстрый доступ к настройкам и интеграциям

**Доступные действия:**

| Иконка | Действие | Описание |
|--------|----------|----------|
| 📁 | Просмотр Google Drive | Открыть файлы проекта |
| 📊 | Управление таблицами | Настройка Google Sheets |
| 🎓 | Обучение агента | Добавить новые команды |

**Индикатор статуса:**
- ✅ Зеленый значок - Google Drive подключен
- ❌ Красный значок - Нет подключения

---

## Функции режима "Разработчик"

### Вкладка "Чат" 💻

**Назначение:** Создание новых функций через диалог с AI

**Отличия от Агент-чата:**
- Ориентирован на разработку кода
- Ответы включают технические детали
- Автоматически логирует процесс разработки

**Примеры запросов:**
```
"Создай функцию для экспорта таблицы в PDF"
"Добавь валидацию email при добавлении строки"
"Реализуй автоматическую синхронизацию каждые 5 минут"
```

---

### Вкладка "Логи" 📋

**Назначение:** Мониторинг процесса разработки в реальном времени

**Отображаемая информация:**
- Временные метки для каждого события
- Иконки статуса:
  - ✅ Успешное выполнение
  - ℹ️ Информационное сообщение
  - ❌ Ошибка
  - 🔨 Начало разработки

**Пример логов:**
```
[14:05:22] ✅ Система запущена
[14:05:23] ℹ️ Подключение к Google Drive...
[14:05:24] ✅ Подключение установлено
[14:10:15] 🔨 Начата разработка новой функции
```

---

### Вкладка "Git" 📊

**Назначение:** Управление версионированием кода

**Отображаемая информация:**
- Текущая ветка (badge с названием)
- Последние коммиты с:
  - Сообщением коммита
  - Временем создания
  - Иконкой типа (🔨 feat, 🐛 fix)

**Действия:**
- Кнопка "🔄 Обновить статус" - проверяет git status

**TODO:** Реализовать интеграцию с реальным Git API

---

## Дизайн-система

### Стиль кнопок

Кнопки используют **glassmorphism** эффект, аналогичный ScriptRunnerMenu:

```tsx
// Основная кнопка
className="button-rounded btn-primary"

// Вторичная кнопка
style={{
  backgroundColor: 'var(--surface)',
  border: '1px solid var(--border)'
}}
```

### Цветовая палитра

| Элемент | Цвет | Использование |
|---------|------|---------------|
| Primary | `#B8C5F2` | Активные элементы, кнопки |
| Secondary | `#E8D5F2` | Вторичные действия |
| Success | `#C5E8D5` | Успешные операции |
| Info | `#D5E8F9` | Информационные сообщения |
| Warning | `#F9E5C8` | Предупреждения |

### Анимации

- `animate-fade-in` - появление сообщений
- `animate-pulse` - индикаторы статуса
- `hover:scale-[1.02]` - увеличение при наведении

---

## API Endpoints

### Агент

**POST** `/api/agents/chat`

```json
{
  "prompt": "Добавь строку с данными...",
  "spreadsheet_id": "1CpYYLvRYslsyCkuLzL9EbbjsvbNpWCEZcmhKqMoX5zw",
  "sheet_name": "Sheet1",
  "context": {}
}
```

**Ответ:**
```json
{
  "status": "success",
  "message": "Строка успешно добавлена!",
  "action_taken": "ADD_ROW"
}
```

### Drive

**GET** `/api/drive/files?folder_id=xyz`

Возвращает список файлов в указанной папке.

---

## TODO: Будущие улучшения

### История чатов
- [ ] Сохранение истории в Google Drive (специальная папка)
- [ ] Синхронизация между устройствами
- [ ] Поиск по истории

### Прикрепление документов
- [ ] Upload файлов на Google Drive
- [ ] OCR для изображений
- [ ] Парсинг PDF/Excel документов

### Git интеграция
- [ ] Просмотр diff
- [ ] Создание коммитов из интерфейса
- [ ] Pull/Push операции

### Логи разработки
- [ ] Фильтрация по уровню (info/error/warning)
- [ ] Экспорт логов в файл
- [ ] Поиск по логам

---

## Конфигурация проектов

Настройки для каждого проекта задаются в `PROJECT_CONFIG`:

```typescript
const PROJECT_CONFIG = {
  sk: {
    sheetId: '1CpYYLvRYslsyCkuLzL9EbbjsvbNpWCEZcmhKqMoX5zw',
    sheetName: 'Sheet1'
  },
  mt: {
    sheetId: '1fMOjUE7oZV96fCY5j5rPxnhWGJkDqg-GfwPZ8jUVgPw',
    sheetName: 'Sheet1'
  }
};
```

---

## Сочетания клавиш

| Действие | Клавиша |
|----------|---------|
| Отправить сообщение | `Enter` |
| Новая строка в сообщении | `Shift + Enter` |
| Прикрепить файл | Кнопка 📎 |

---

## Устранение неполадок

### Агент не отвечает
1. Проверьте подключение к интернету
2. Убедитесь что backend запущен на `localhost:8000`
3. Проверьте переменную `GEMINI_API_KEY`

### Ошибка "Module not found"
1. Перезапустите dev-сервер: `npm run dev`
2. Очистите кэш Next.js: `rm -rf .next`

### Google Drive не подключается
1. Проверьте `credentials.json`
2. Убедитесь что `GOOGLE_APPLICATION_CREDENTIALS` настроен правильно

---

## Для разработчиков

### Добавление новой вкладки

1. Добавьте тип в `Tab`:
```typescript
type Tab = 'chat' | 'history' | 'controls' | 'mynew-tab';
```

2. Добавьте в `visibleTabs`:
```typescript
{ id: 'mynew-tab', label: 'Моя вкладка', icon: '🎯' }
```

3. Добавьте рендеринг контента:
```typescript
{activeTab === 'mynew-tab' && (
  <div>Мой контент</div>
)}
```

### Добавление нового проекта

Обновите `PROJECT_CONFIG` с новыми данными:
```typescript
newproject: {
  sheetId: 'your-sheet-id',
  sheetName: 'Sheet1'
}
```

---

## Заключение

Панель Агента и Разработчика предоставляет мощный инструмент для управления проектом через AI и современные инструменты разработки. Следуйте этому руководству для максимальной эффективности работы!

**Версия:** 1.0
**Дата обновления:** 08.12.2025
