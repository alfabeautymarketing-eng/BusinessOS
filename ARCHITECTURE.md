# Архитектура BusinessOS

**Дата последнего обновления**: 2025-12-09
**Версия**: 2.2
**Стиль дизайна**: iOS с пастельными цветами

---

## Содержание

1. [Обзор структуры](#обзор-структуры)
2. [Компоненты](#компоненты)
3. [Система стилей](#система-стилей)
4. [Правила разработки](#правила-разработки)
5. [История изменений](#история-изменений)

---

## Обзор структуры

### Иерархия компонентов

```
App (page.tsx)
├── Shell.tsx                    ← Основной контейнер с тремя панелями
│   ├── TopNav.tsx              ← Верхняя навигация
│   ├── ScriptRunnerMenu.tsx    ← Левая панель (функции)
│   ├── Main Content            ← Центральная область
│   │   ├── Workspace Header    ← Заголовок "Рабочие столы"
│   │   ├── Workspace Buttons   ← Кнопки переключения столов
│   │   ├── TabsBar.tsx        ← Панель вкладок
│   │   └── ContentViewer.tsx  ← Просмотр содержимого (iframe)
│   └── AgentSidebar.tsx        ← Правая панель (агент/логи)
```

---

## Компоненты

### 1. Shell.tsx
**Назначение**: Основной контейнер приложения с тремя колонками

#### Особенности:
- Ресайзеры для изменения ширины панелей
- Пастельный градиентный фон
- Мягкие блики в стиле iOS

#### Классы:
- `card-static` - для header, боковых панелей и main
- Без hover-эффектов на больших панелях

#### Код:
```tsx
<Shell
  topNav={<TopNav />}
  sidebar={<ScriptRunnerMenu />}
  rightSidebar={<AgentSidebar />}
  showSidebar={true}
  showRightSidebar={true}
>
  {children}
</Shell>
```

---

### ⚠️ ОБЯЗАТЕЛЬНАЯ ФУНКЦИЯ: Ресайзеры панелей

#### Назначение:
Пользователь должен иметь возможность изменять ширину левой и правой боковых панелей с помощью мыши.

#### Технические детали:

**Левая панель (ScriptRunnerMenu):**
- Ширина по умолчанию: `300px`
- Минимальная ширина: `240px`
- Максимальная ширина: `450px`
- Ручка ресайзера: справа от панели, 24px ширина
- Код: `Shell.tsx` строки 66-77

**Правая панель (AgentSidebar):**
- Ширина по умолчанию: `360px`
- Минимальная ширина: `280px`
- Максимальная ширина: `450px`
- Ручка ресайзера: слева от панели, 24px ширина
- Код: `Shell.tsx` строки 88-98

#### Визуальный стиль ручки:
```tsx
<div className="w-1.5 h-12 rounded-full bg-gray-400/30
                group-hover:bg-[var(--primary)]
                transition-colors backdrop-blur-md shadow-sm" />
```

#### Поведение:
1. При наведении мыши на ручку - она подсвечивается основным цветом (`var(--primary)`)
2. При захвате ручки - курсор меняется на `col-resize`
3. Во время изменения размера блокируется выделение текста (`user-select: none`)
4. Размер обновляется в реальном времени
5. При отпускании кнопки мыши - ресайз завершается

#### Состояние:
```tsx
const [leftWidth, setLeftWidth] = useState(300);
const [rightWidth, setRightWidth] = useState(360);
const [resizing, setResizing] = useState<'left' | 'right' | null>(null);
```

#### Защита от конфликтов с iframe:
При активном ресайзе создается глобальный оверлей, который перехватывает события мыши над iframe'ами:
```tsx
{resizing && (
  <div className="fixed inset-0 z-[9999] cursor-col-resize user-select-none" />
)}
```

---

### 2. page.tsx (Main Content)
**Назначение**: Центральная область с вкладками и контентом

#### Структура:
1. **TabsBar** - панель вкладок документов
2. **ContentViewer** - отображение содержимого

**Примечание**: Переключатель рабочих столов теперь находится в TopNav (верхний колонтитул)

---

### 3. TopNav.tsx
**Назначение**: Верхняя навигация с логотипом, переключателем рабочих столов, статусами и управлением

#### Элементы:
1. **Логотип** (💼 Business OS)
2. **Переключатель рабочих столов** - компактные кнопки с эмодзи
   - Активный стол: увеличен в 1.5 раза (`scale-150`) + подсветка
   - Все столы в едином стиле с `card-glass` эффектом
3. **Кнопки управления раскладкой** (все/центр/слева/справа)
4. **Статусные бейджи** (Health, Система в сети)
5. **Аватар пользователя** (👤)

---

### ⚠️ ОБЯЗАТЕЛЬНАЯ ФУНКЦИЯ: Кнопки управления раскладкой

#### Назначение:
Пользователь должен иметь возможность управлять видимостью боковых панелей с помощью 3 кнопок в верхнем правом углу интерфейса.

#### Технические детали:

**Расположение:** Правая часть TopNav компонента (строки 162-186 в `TopNav.tsx`)

#### 3 Кнопки:

**1. Кнопка "Full View" (Все панели)**
- Иконка: `<Layout size={18} />` (lucide-react)
- Режим: `'all'`
- Действие: Показывает обе боковые панели (левую и правую)
- Title: "Full View"
- Код: строки 164-169

**2. Кнопка "Hide Left" (Скрыть левую)**
- Иконка: `<LayoutPanelLeft size={18} />` (lucide-react)
- Режим: `'left'`
- Действие: Скрывает левую панель (ScriptRunnerMenu), показывает правую
- Title: "Hide Left"
- Код: строки 171-176

**3. Кнопка "Hide Right" (Скрыть правую)**
- Иконка: `<Square size={18} />` (lucide-react)
- Режим: `'right'`
- Действие: Скрывает правую панель (AgentSidebar), показывает левую
- Title: "Hide Right"
- Код: строки 178-184

#### Визуальный стиль:

**Контейнер кнопок:**
```tsx
<div className="bg-white/40 p-1 rounded-xl border border-white/40
                flex items-center gap-1 backdrop-blur-sm">
```

**Активная кнопка:**
```tsx
className="p-1.5 rounded-lg transition-all
           bg-white shadow-sm text-indigo-600"
```

**Неактивная кнопка:**
```tsx
className="p-1.5 rounded-lg transition-all
           text-gray-400 hover:text-gray-600 hover:bg-white/20"
```

#### Логика в page.tsx:

```tsx
const [layoutMode, setLayoutMode] = React.useState<'all' | 'center' | 'left' | 'right'>('all');

const showSidebar = layoutMode === 'all' || layoutMode === 'left';
const showRightSidebar = layoutMode === 'all' || layoutMode === 'right';

<Shell
  showSidebar={showSidebar}
  showRightSidebar={showRightSidebar}
>
```

#### Поведение:
1. Нажатие на кнопку меняет `layoutMode` состояние
2. Shell компонент получает `showSidebar` и `showRightSidebar` props
3. Панели плавно появляются/исчезают с `transition-all duration-300`
4. Активная кнопка подсвечивается белым фоном и синим цветом иконки
5. Hover эффект на неактивных кнопках

#### Пример кнопки рабочего стола:
```tsx
<button
  className={`flex items-center gap-2 px-4 py-2 text-xs font-semibold
              transition-all duration-300 rounded-xl border-2
              ${isActive ? 'scale-150' : 'scale-100'}`}
  style={{
    borderColor: isActive ? 'var(--primary)' : 'var(--border)',
    backgroundColor: 'var(--surface-glass)',
    color: isActive ? 'var(--text-primary)' : 'var(--text-secondary)',
    boxShadow: isActive ? '0 0 20px rgba(184, 197, 242, 0.5), var(--shadow-lg)' : 'var(--shadow-sm)',
    backdropFilter: 'blur(20px)'
  }}
>
  <span className="h-2.5 w-2.5 rounded-full"
        style={{
          background: project.color,
          boxShadow: isActive ? `0 0 12px ${project.color}` : 'none'
        }}
  />
  <span>{project.icon}</span>
</button>
```

#### Правила:
- Используйте `card-glass` для логотипа и контейнеров
- Все иконки с эмодзи
- Бейджи с `var(--success)` цветом
- Активный рабочий стол увеличен и подсвечен

---

### 4. TabsBar.tsx
**Назначение**: Панель открытых вкладок документов

#### Состояния вкладки:
- **Активная**: `scale-105`, border с `var(--primary)`, card-glass
- **Неактивная**: `scale-100`, прозрачная, border на hover
- **Кнопка закрытия**: Появляется на hover, розовый фон

#### Пустое состояние:
```tsx
<div className="card-glass inline-flex items-center gap-2">
  <span className="text-base">✨</span>
  <span>Выберите проект из меню выше</span>
</div>
```

---

### 5. ScriptRunnerMenu.tsx
**Назначение**: Левая панель с функциями и скриптами

#### Структура:
- **Header**: Заголовок с индикатором
- **Дерево функций**: Раскрывающиеся секции в компактном стиле
- **Footer**: Статус системы

#### Стиль "вкладок" для функций:
- Секции и элементы в едином компактном стиле
- Используется `card-glass` эффект для активных элементов
- Скругленные углы (`rounded-lg`)
- Эмодзи вместо иконок
- Border и shadow для выделения активных элементов

#### Секции:
```tsx
<button
  className={`w-full flex items-center gap-2 px-3 py-2 text-xs font-semibold
             transition-all duration-200 rounded-lg border
             ${isExpanded ? 'card-glass' : ''}`}
  style={{
    backgroundColor: isExpanded ? 'var(--surface-glass)' : 'var(--surface-glass)',
    borderColor: isExpanded ? 'var(--primary)' : 'var(--border)',
    boxShadow: isExpanded ? 'var(--shadow-md)' : 'var(--shadow-sm)'
  }}
>
  <svg>{/* Стрелка */}</svg>
  <span className="text-sm">{section.icon}</span>
  <span>{section.title}</span>
</button>
```

#### Элементы меню:
```tsx
<button
  className="w-full flex items-center gap-2 px-3 py-2 text-xs font-medium
            transition-all duration-200 rounded-lg border"
  onMouseEnter={(e) => {
    e.currentTarget.style.backgroundColor = 'var(--surface-glass)';
    e.currentTarget.style.borderColor = 'var(--border)';
    e.currentTarget.style.boxShadow = 'var(--shadow-sm)';
  }}
>
  <span className="text-sm">{item.icon}</span>
  <span>{item.label}</span>
</button>
```

---

### 6. AgentSidebar.tsx
**Назначение**: Правая панель с чат-ботом и логами

#### Вкладки:
1. **💬 Чат** - общение с агентом
2. **📋 Логи** - журнал событий
3. **📊 Git** - история Git
4. **⚙️ Управление** - быстрые действия

#### Особенности чата:
- Сообщения пользователя: розовый border (`var(--secondary)`)
- Сообщения агента: синий border (`var(--primary)`)
- Инпут: класс `input-ios`
- Кнопка отправки: `button-rounded btn-primary`

---

### 7. ContentViewer.tsx
**Назначение**: Отображение содержимого во iframe

#### Пустое состояние:
```tsx
<div className="card-glass p-12 max-w-md animate-fade-in">
  <div className="text-7xl mb-4">📋</div>
  <p>Нет активных вкладок</p>
  <div className="flex gap-3 mt-6">
    <div className="badge">📊 Google Таблицы</div>
    <div className="badge">🤖 Чат-боты</div>
  </div>
</div>
```

#### С контентом:
- Светлый фон `var(--surface)`
- Iframe на весь размер

---

## Система стилей

### CSS-переменные (globals.css)

#### Цвета:
```css
--primary: #B8C5F2        /* 💜 Лавандовый */
--secondary: #E8D5F2      /* 🦄 Сирень */
--accent: #F2D5E5         /* 💗 Розовый */
--success: #C5E8D5        /* 🌿 Мятный */
--warning: #F9E5C8        /* 🍑 Персиковый */
--info: #D5E8F9           /* ☁️ Голубой */
```

#### Классы:

##### Кнопки:
```css
.button-rounded          /* Полукруглые края + padding */
.btn-primary            /* Лавандовый градиент */
.btn-secondary          /* Сиреневый градиент */
.btn-success            /* Мятный градиент */
```

##### Карточки:
```css
.card-glass             /* С hover-эффектом (для кнопок) */
.card-static            /* Без hover (для панелей) */
```

##### Инпуты:
```css
.input-ios              /* Стиль iOS с фокусом */
```

##### Прочее:
```css
.badge                  /* Статусные бейджи */
.divider                /* Разделители */
.tooltip                /* Подсказки при hover */
.animate-fade-in        /* Плавное появление */
```

---

## Правила разработки

### 1. Использование цветов

#### ✅ Правильно:
```tsx
<div style={{ color: 'var(--text-primary)' }}>Текст</div>
```

#### ❌ Неправильно:
```tsx
<div className="text-[#2E3A4D]">Текст</div>
```

---

### 2. Выбор класса карточки

#### Используйте `card-glass`:
- Кнопки
- Маленькие элементы
- Элементы, которые должны реагировать на hover

#### Используйте `card-static`:
- Большие панели (Shell header, sidebar, main)
- Контейнеры
- Элементы без hover-эффектов

---

### 3. Кнопки переключения рабочих столов (в TopNav)

#### Активная кнопка:
- Класс: `scale-150` (увеличена в 1.5 раза)
- Border: `var(--primary)`
- Background: `var(--surface-glass)`
- Shadow: Усиленная подсветка с glow эффектом
- Backdrop blur: `blur(20px)`

#### Неактивная кнопка:
- Класс: `scale-100` (обычный размер)
- Border: `var(--border)`
- Background: `var(--surface-glass)`
- Shadow: `var(--shadow-sm)`
- Backdrop blur: `blur(20px)`

**Примечание**: Все кнопки в едином стиле, отличается только размер и интенсивность подсветки

---

### 5. Эмодзи

Используйте эмодзи для визуальной привлекательности:

#### Бизнес:
- 💼 Проекты
- 📊 Аналитика
- 📈 Статистика
- 🗂️ Рабочие столы

#### Коммуникация:
- 💬 Чаты
- 🤖 Боты
- 👤 Пользователь

#### Действия:
- ✅ Готово
- ⚙️ Настройки
- 📋 Документы
- 🌐 Сеть

---

### 6. Адаптивность

Используйте Tailwind классы для адаптивности:

```tsx
<div className="p-4 md:p-6 lg:p-8">
  {/* Отступы меняются на разных экранах */}
</div>
```

---

## История изменений

### Версия 2.2 (2025-12-09)
**Документация обязательных функций интерфейса**

#### Добавлено:
- ✅ **Обязательная функция:** Документация ресайзеров панелей
- ✅ **Обязательная функция:** Документация 3 кнопок управления раскладкой
- ✅ Подробное описание технических деталей в ARCHITECTURE.md
- ✅ Руководство пользователя в README.md
- ✅ Спецификации диапазонов ширины панелей (240-450px левая, 280-450px правая)
- ✅ Описание визуальных подсказок и поведения

#### Зафиксировано:
- 📌 Ресайзеры панелей - обязательная функция для настройки рабочего пространства
- 📌 3 кнопки управления раскладкой - обязательная функция для управления видимостью панелей
- 📌 Технические требования к реализации
- 📌 Горячие клавиши (в планах на будущее)

### Версия 2.1 (2025-12-03)
**Оптимизация UI и компактный дизайн**

#### Добавлено:
- ✅ Переключатель рабочих столов в TopNav (верхний колонтитул)
- ✅ Увеличение активного стола в 1.5 раза (`scale-150`)
- ✅ Единый стиль для всех рабочих столов (активных и неактивных)
- ✅ Подсветка с glow эффектом для активного стола
- ✅ Компактный стиль "вкладок" для ScriptRunnerMenu
- ✅ Эмодзи вместо иконок в левой панели

#### Изменено:
- 🔄 page.tsx: Удален заголовок "Рабочие столы"
- 🔄 page.tsx: Удален переключатель рабочих столов (перемещен в TopNav)
- 🔄 TopNav.tsx: Добавлен компактный переключатель рабочих столов
- 🔄 ScriptRunnerMenu.tsx: Упакован в стиле вкладок с `card-glass`

#### Удалено:
- ❌ Секция "Рабочие столы" из центральной области
- ❌ Старый стиль кнопок рабочих столов (разные границы для активных/неактивных)

### Версия 2.0 (2025-12-03)
**Обновление дизайна в стиле iOS**

#### Добавлено:
- ✅ Пастельная цветовая палитра
- ✅ Класс `card-static` без hover для панелей
- ✅ Заголовок "Рабочие столы" с эмодзи
- ✅ Улучшенный дизайн кнопок переключения столов
- ✅ Окантовка для неактивных кнопок
- ✅ Пустое состояние ContentViewer с бейджами

#### Изменено:
- 🔄 ContentViewer: темный фон → светлый
- 🔄 Shell: темные панели → светлые с card-static
- 🔄 Все компоненты: темная тема → светлая пастель

#### Удалено:
- ❌ Темные градиенты
- ❌ Hover-эффекты на больших панелях

### Версия 1.0 (до 2025-12-03)
- Темная тема с голубыми/фиолетовыми акцентами

---

## Важные файлы

### Стили:
- `client/src/app/globals.css` - Все CSS-переменные и классы

### Компоненты:
- `client/src/app/page.tsx` - Главная страница
- `client/src/components/Shell.tsx` - Основной контейнер
- `client/src/components/TopNav.tsx` - Верхняя навигация
- `client/src/components/TabsBar.tsx` - Панель вкладок
- `client/src/components/ContentViewer.tsx` - Просмотр содержимого
- `client/src/components/AgentSidebar.tsx` - Правая панель
- `client/src/components/ScriptRunnerMenu.tsx` - Левая панель

### Конфигурация:
- `client/src/config/projects.json` - Список проектов/рабочих столов

---

## Команды для разработки

```bash
# Запуск dev-сервера
npm run dev

# Сборка проекта
npm run build

# Проверка типов
npm run type-check

# Форматирование кода
npm run format
```

---

**Помните**: Всегда обновляйте эту документацию при внесении изменений в архитектуру!
