# Business OS: Агентская IDE для Google Таблиц

## 1. Обзор Проекта (Project Overview)

**Business OS** — это веб-приложение нового поколения, которое переосмысливает работу с Google Таблицами. Вместо того чтобы просто редактировать ячейки, пользователь получает полноценную "Операционную Систему" для бизнеса, где Google Таблица выступает в роли IDE (Интегрированной Среды Разработки), а Искусственный Интеллект — в роли штатного программиста.

### Основная Идея
Пользователь не должен писать формулы или скрипты вручную. Он ставит задачу на естественном языке (например, *"Посчитай маржу для всех товаров из категории 'Косметика' и выдели красным те, где маржа < 20%"*), а Агент автоматически пишет код, тестирует его и внедряет в таблицу.

---

## 2. Исходный Запрос Пользователя (Initial User Request)

Проект родился из потребности объединить управление бизнес-процессами (SK, MT, SS) в единый, удобный интерфейс.

**Ключевые требования:**
1.  **Веб-оболочка**: Приложение должно оборачивать Google Таблицу, скрывая лишний интерфейс Google и добавляя свой ("Кокпит").
2.  **Умная Рамка (Smart Overlay)**: Интерактивный слой поверх таблицы. Клик по заголовку колонки должен "захватывать" её контекст (ID, имя) для передачи Агенту.
3.  **Агент-Кодер**: ИИ, который пишет Google Apps Script.
4.  **Агент-Исполнитель**: ИИ, который выполняет рутинные действия.
5.  **Интеграция**: Поддержка существующих проектов (`MyGoogleScripts`, `cosmetic-agent`).
6.  **DevOps**: Автоматическое сохранение кода в Git и деплой в Google Таблицы (через CLASP/API).
7.  **Дизайн**: Тема "Deep Dark", премиальный вид, анимации.

---

## 3. Архитектура Системы

Мы реализовали современную архитектуру монорепозитория:

### 3.1 Frontend ("Кокпит")
*   **Технологии**: Next.js 14, React, Tailwind CSS, TypeScript.
*   **Расположение**: `/client`
*   **Ключевые Компоненты**:
    *   `Shell`: Основной макет с темной темой.
    *   `SheetOverlay`: Визуальный слой поверх `iframe` с Google Таблицей. Отображает интерактивные теги колонок.
    *   `AgentSidebar`: Чат с ИИ, логи выполнения, просмотр сгенерированного кода.
    *   `TopNav`: Навигация между проектами (SK, MT, SS) и внешними модулями.

### 3.2 Backend ("Мозг")
*   **Технологии**: Python, FastAPI, Google API Client.
*   **Расположение**: `/server`
*   **Модули**:
    *   `api/auth`: Авторизация через Google Service Account.
    *   `api/sheets`: Чтение структуры таблиц для отрисовки Оверлея.
    *   `api/drive`: Файловый менеджер для навигации по проектам.
    *   `api/agents`: Интеграция с LLM (Gemini) и Google Apps Script API.

### 3.3 Интеграции
*   **Google Sheets API**: Чтение/запись данных.
*   **Google Apps Script API**: Программный деплой кода (замена `clasp push`).
*   **Gemini 1.5 Pro**: Генерация кода и анализ данных.

---

## 4. Реализованные Этапы (Implemented Stages)

### ✅ Фаза 1: Инициализация
*   Создана структура монорепозитория.
*   Проведен анализ существующих проектов (`MyGoogleScripts`, `cosmetic-agent`).

### ✅ Фаза 2: Frontend
*   Разработан интерфейс "Кокпит" в стиле Deep Dark.
*   **Реализована концепция "Рабочих столов" (Workspaces)**:
    *   Каждая вкладка проекта (SkinClinic, Montibello, Soskin) создает изолированный контекст.
    *   **Изолированный Чат**: История переписки с Агентом сохраняется отдельно для каждого проекта.
    *   **Адаптивный UI**: Интерфейс меняет цветовые акценты (Фиолетовый/Синий/Зеленый) в зависимости от активного проекта.
*   **Иерархическое Меню Функций**:
    *   Реализовано древовидное меню (как в Google Drive) для запуска скриптов.
    *   Меню контекстно-зависимое и отображает функции, актуальные для текущего проекта.
*   Реализован компонент `SheetOverlay` (прототип).
*   Создана боковая панель для общения с Агентом.

### ✅ Фаза 3: Backend & Google
*   Настроен сервер FastAPI.
*   Реализована аутентификация через Service Account (`cosmetic-agent`).
*   Подключены API: Drive, Sheets.

### ✅ Фаза 4: Агент & Деплой
*   **Генерация Кода**: Подключен Gemini API. Агент умеет писать скрипты по текстовому промпту.
*   **Деплой**: Реализован механизм `update_script_content`, который пушит код напрямую в Google Apps Script проект, минуя консольные утилиты.

### ✅ Фаза 5: Инфраструктура
*   Создан скрипт `start.sh` для запуска всего проекта одной командой.
*   Создан скрипт `deploy.sh` для автоматизации Git.
*   Подготовлен `render.yaml` для деплоя в облако.

---

## 5. План Развития (Roadmap)

### 🚧 Ближайшие шаги (To-Do)
1.  **Интерактивность Оверлея**: Сделать так, чтобы клик по тегу колонки реально передавал её ID в чат Агента (сейчас это визуальная заглушка).
2.  **Подключение `cosmetic-agent`**: Встроить существующий дашборд как `iframe` или отдельную страницу внутри Business OS.
3.  **E2E Тестирование**: Провести полный цикл: "Запрос -> Генерация -> Деплой -> Результат в таблице".

### 🔮 Будущее
*   **Голосовое управление**: Добавить возможность диктовать задачи.
*   **Мульти-агентность**: Разделение на "Архитектора" (планирует структуру скрипта) и "Кодера" (пишет код).
*   **Локальная LLM**: Возможность подключения Ollama для работы без интернета/расходов на API.

---

## 6. Инструкция по Запуску

### Локальный запуск
```bash
# 1. Перейти в папку проекта
cd BusinessOS

# 2. Запустить одной командой
./start.sh
```
Приложение будет доступно по адресу: `http://localhost:3000`

### Деплой
```bash
# Отправка изменений на GitHub
./deploy.sh
```
